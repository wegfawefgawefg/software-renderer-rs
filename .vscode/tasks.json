{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo run --release",
      "type": "shell",
      "command": "cargo run --release",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$rustc"]
    }
  ]
}

